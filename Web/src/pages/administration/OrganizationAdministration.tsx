import { useEffect, useState } from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { multiTenantService, OrganizationRecord } from "@/services/api/multiTenantService";
export default function OrganizationAdministration(){const[profile,setProfile]=useState<OrganizationRecord|null>(null);const[tenants,setTenants]=useState<Awaited<ReturnType<typeof multiTenantService.organizationTenants>>>([]);const[admins,setAdmins]=useState<Awaited<ReturnType<typeof multiTenantService.organizationAdministrators>>>([]);const[subscriptions,setSubscriptions]=useState<Awaited<ReturnType<typeof multiTenantService.organizationSubscriptions>>>([]);useEffect(()=>{Promise.all([multiTenantService.organizationProfile(),multiTenantService.organizationTenants(),multiTenantService.organizationAdministrators(),multiTenantService.organizationSubscriptions()]).then(([p,t,a,s])=>{setProfile(p);setTenants(t);setAdmins(a);setSubscriptions(s);});},[]);return <div className="space-y-6 p-6"><div><h1 className="text-2xl font-semibold">Organization Administration</h1><p className="text-muted-foreground">Persisted customer identity, defaults, administrators, subscription history, and tenants.</p></div><Tabs defaultValue="profile"><TabsList><TabsTrigger value="profile">Profile</TabsTrigger><TabsTrigger value="tenants">Tenants</TabsTrigger><TabsTrigger value="admins">Administrators</TabsTrigger><TabsTrigger value="subscription">Subscription</TabsTrigger></TabsList><TabsContent value="profile"><Card><CardHeader><CardTitle>{profile?.displayName??"Organization"}</CardTitle></CardHeader><CardContent className="grid gap-2 text-sm"><div>Legal name: {profile?.legalName}</div><div>Currency: {profile?.currency}</div><div>Timezone: {profile?.timezone}</div><div>Locale: {profile?.locale}</div><div>Date format: {profile?.dateFormat}</div></CardContent></Card></TabsContent><TabsContent value="tenants"><Card><CardContent className="pt-6">{tenants.map(x=><div className="border-b py-3" key={x.id}>{x.name} <span className="text-muted-foreground">({x.identifier})</span></div>)}</CardContent></Card></TabsContent><TabsContent value="admins"><Card><CardContent className="pt-6">{admins.map(x=><div className="border-b py-3" key={x.id}>{x.userName} · {x.email}</div>)}</CardContent></Card></TabsContent><TabsContent value="subscription"><Card><CardContent className="pt-6">{subscriptions.map(x=><div className="border-b py-3" key={x.id}>{x.name} · {x.status} · {x.currency}</div>)}</CardContent></Card></TabsContent></Tabs></div>}
